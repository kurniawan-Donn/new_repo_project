<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.My_aplication">

        <!-- Main Activity -->
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Tambah Catatan Activity -->
        <activity
            android:name=".TambahCatatanActivity"
            android:exported="false" />

        <!-- Tambah Tugas Activity -->
        <activity
            android:name=".TambahTugasActivity"
            android:exported="false" />

        <!-- Catatan Activity (untuk isi deskripsi) -->
        <activity
            android:name=".CatatanActivity"
            android:exported="false" />

    </application>

</manifest>